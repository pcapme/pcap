syntax = "proto3";

package pcapserver;

service PCAP {
  rpc Listen (ListenRequest) returns (ListenReply) {}
}

message ListenRequest {
    string filter = 1;
    string interface = 2;
}

message ListenReply {
    bool success = 1;
}
